using System.Windows.Controls;

namespace Illusion.Views;

/// <summary>The SDS tab: what the selected archive holds, in two numbers.</summary>
public partial class SdsTabView : UserControl
{
    public SdsTabView() => InitializeComponent();
}
