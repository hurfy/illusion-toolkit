namespace Illusion.Rendering.Gizmos;

/// <summary>Which manipulation the transform gizmo performs (driven by the viewport tool shelf).</summary>
public enum GizmoMode
{
    None,
    Move,
    Rotate,
    Scale,
}
